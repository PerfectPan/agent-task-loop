---
"@rivus/agent-task-loop": patch
---

Add an `agent-task-loop create` command for scriptable task creation, with required flags, interactive prompting in TTYs, source selection, validation, and JSON output.
