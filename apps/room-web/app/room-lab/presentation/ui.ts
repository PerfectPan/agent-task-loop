/**
 * The room's one shared Tailwind cluster: controls come from `~/components/ui/*`,
 * so only this typography cluster is left to share. The scales it draws on are
 * specified in DESIGN.md.
 */

/** Section label in the rail and the context column. Quiet, never uppercase. */
export const sectionLabel = 'm-0 text-xs font-medium text-muted-foreground';
