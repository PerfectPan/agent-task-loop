{
  "name": "@rivus/agent-orchestration",
  "version": "0.0.0",
  "private": true,
  "description": "Seats, tokens, and a channel for a multi-agent run.",
  "license": "GPL-3.0-only",
  "type": "module",
  "bin": {
    "orch": "./bin/orch.mjs"
  },
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "development": "./src/index.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "bin/",
    "dist/",
    "src/**/*.ts",
    "README.md"
  ],
  "scripts": {
    "build": "rslib build",
    "typecheck": "tsc --noEmit",
    "prepack": "pnpm build",
    "test": "rslib build && vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@rsbuild/core": "^2.0.3",
    "@rslib/core": "^0.21.3",
    "@rivus/rslib-config": "workspace:*",
    "@types/node": "^22.15.3",
    "tsx": "^4.19.2",
    "typescript": "^5.8.3",
    "vitest": "^3.1.2"
  },
  "engines": {
    "node": ">=22.13"
  }
}
