# Changesets

Add a changeset for user-facing package changes:

```bash
pnpm changeset
```

Documentation-only changes, repository metadata changes, and internal maintenance that does not affect a published package can skip a changeset.
